node_modules
.DS_Store
dist
dist-ssr
playwright-report
test-results
*.local
.cursorrules

# Vite generates these declarations, but they are committed because vue-tsc runs before Vite during build.
.auto-import.json

